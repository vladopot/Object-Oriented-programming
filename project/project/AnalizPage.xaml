<Page x:Class="project.AnalizPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:project"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AnalizPage">
    <Page.Background>
        <ImageBrush ImageSource="Resources/Background.jpg"></ImageBrush>
    </Page.Background>
    <Page.Resources>
        <Style x:Key="BtnStyle" TargetType="Button">
            <Setter Property="Background" Value="LightGray"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="Black"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder"
Background="{TemplateBinding Background}"
BorderBrush="{TemplateBinding BorderBrush}"
BorderThickness="{TemplateBinding BorderThickness}" >
                            <ContentPresenter HorizontalAlignment="Center"
              VerticalAlignment="Center"
              Content="{TemplateBinding Content}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="ButtonBorder"
                            Storyboard.TargetProperty="(Background).(SolidColorBrush.Color)"
                            To="Gray"
                            Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation Storyboard.TargetName="ButtonBorder"
                            Storyboard.TargetProperty="(Background).(SolidColorBrush.Color)"
                            To="LightGray"
                            Duration="0:0:0.2"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <Button Click="NavigateTo" Grid.Column="0" Style="{StaticResource BtnStyle}" x:Name="HistoryBtn" Content="History" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Height="60" FontSize="16" Margin="0,0,0,180"></Button>
        <Button Click="NavigateTo" Grid.Column="0" Style="{StaticResource BtnStyle}" x:Name="AnalyzBtn" Content="Analyz" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Height="60" FontSize="16" Margin="0,0,0,120"></Button>
        <Button Click="NavigateTo" Grid.Column="0" Style="{StaticResource BtnStyle}" x:Name="plansBtn" Content="Plans" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Height="60" FontSize="16" Margin="0,0,0,60"></Button>
        <Button Click="NavigateTo" Grid.Column="0" Style="{StaticResource BtnStyle}" x:Name="exitBtn" IsCancel="True" Content="Back" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Height="60" FontSize="16" Margin="0,0,0,0"></Button>

        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Analyzing and calculating the calorie intake" Foreground="White" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Goal: " Foreground="White" VerticalAlignment="Center" FontSize="16" Width="100"/>
                <ComboBox x:Name="GoalComboBox" Width="200" FontSize="16" SelectedIndex="0">
                    <ComboBoxItem Content="Gain Weight"/>
                    <ComboBoxItem Content="Maintain Weight"/>
                    <ComboBoxItem Content="Lose Weight"/>
                </ComboBox>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Weight (kg): " Foreground="White" VerticalAlignment="Center" FontSize="16" Width="100"/>
                <TextBox x:Name="WeightTextBox" Width="200" FontSize="16"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Height (сm): " Foreground="White" VerticalAlignment="Center" FontSize="16" Width="100"/>
                <TextBox x:Name="HeightTextBox" Width="200" FontSize="16"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <TextBlock Text="Age: " Foreground="White" VerticalAlignment="Center" FontSize="16" Width="100"/>
                <TextBox x:Name="AgeTextBox" Width="200" FontSize="16"/>
            </StackPanel>

            <Button Content="Calculate CPFC" Click="CalculateKBJU" FontSize="16" HorizontalAlignment="Center" Width="200" Margin="0,0,0,20"/>

            <StackPanel x:Name="ResultStackPanel" Visibility="Collapsed">
                <TextBlock Text="Results:" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
                <TextBlock x:Name="CaloriesText" Foreground="White" FontSize="16" Margin="0,0,0,5"/>
                <TextBlock x:Name="ProteinsText" Foreground="White" FontSize="16" Margin="0,0,0,5"/>
                <TextBlock x:Name="FatsText" Foreground="White" FontSize="16" Margin="0,0,0,5"/>
                <TextBlock x:Name="CarbsText" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
            </StackPanel>

            <TextBlock Text="Weight change graph:" Foreground="White" FontSize="18" FontWeight="Bold" Margin="0,20,0,10"/>

            <Canvas x:Name="ChartCanvas" Background="White" Margin="0,0,0,20" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Height="150" Width="550"></Canvas>

            <UniformGrid Columns="12" Width="500">
                <TextBlock Text="Jan" Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
                <TextBlock Text="Feb" Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
                <TextBlock Text="Mar" Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
                <TextBlock Text="Apr" Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
                <TextBlock Text="May" Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
                <TextBlock Text="Jun" Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
                <TextBlock Text="Jul" Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
                <TextBlock Text="Aug" Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
                <TextBlock Text="Sep" Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
                <TextBlock Text="Oct" Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
                <TextBlock Text="Nov" Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
                <TextBlock Text="Dec" Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
            </UniformGrid>
        </StackPanel>
    </Grid>
</Page>
